# PWA icons

Export from your square brand logo (or regenerate placeholders with
`node scripts/make-brand-assets.mjs`):

- `icon-192.png` — 192×192
- `icon-512.png` — 512×512 (also advertised as maskable — keep the mark
  within the inner ~80% safe zone)

Referenced by `src/app/manifest.ts`.
